#Cpp 

`new teacher`
==**Templates**== (hw only)
n13
Метод программирования.

==**Smart Pointers**==
n14

Умные указатели (Smart pointer) C++11
Обертка над каким либо типом.
Хранит в себе адрес на какой-либо ресурс.
Инструмент для работы с памятью, передача в функцию по ссылке.

include <memory>

unique_ptr 
управление ресурсами с единственным владельцем
std::move(ptr) присваивание значения

deleter - самостоятельное удаление (напр. закрыть сокет)

shared_ptr
управление ресурсами с несколькими владельцами
+счетчик ссылок
.make_shared<>()
.make_unqiue C++14

weak_ptr
ссылается на shared_ptr, но не увеличивает счетчик ссылок
используется для отслеживания существования объекта

через .get() получить сырой указатель





#Cpp  